server:
  port: 8080

spring:
  ai:
    openai:
      api-key: sk-your-key
      base-url: https://dashscope.aliyuncs.com/compatible-mode
      chat:
        options:
          model: qwen-plus
      embedding:
        options:
          model: text-embedding-v3
          dimensions: 1024
    vectorstore:
      qdrant:
        base-url: http://localhost:6334
        api-key: ""
        init-schema: true
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB
